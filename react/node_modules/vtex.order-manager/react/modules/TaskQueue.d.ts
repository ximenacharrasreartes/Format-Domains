import type { CancellablePromiseLike } from './SequentialTaskQueue';
import { QueueStatus } from '../constants';
export declare class TaskQueue {
    private queue;
    private taskIdMap;
    private listeners;
    private isEmpty;
    constructor();
    isWaiting(id: string): boolean;
    enqueue(task: () => Promise<any>, id?: string): CancellablePromiseLike<any>;
    listen(event: QueueStatus, cb: () => any): () => void;
    private emit;
}
