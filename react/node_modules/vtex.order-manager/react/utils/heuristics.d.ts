import type { OrderForm } from 'vtex.checkout-graphql';
/**
 * Heuristic function to determine whether or not the local
 * order form (stored in localStorage) should be replaced by
 * the remote order form.
 */
export declare const shouldUpdateOrderForm: (localOrderForm: OrderForm, remoteOrderForm: OrderForm) => boolean;
