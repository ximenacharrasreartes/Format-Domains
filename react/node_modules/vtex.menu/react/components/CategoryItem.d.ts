import { Component } from 'react';
import { StyledLinkProps } from './StyledLink';
declare class CategoryItem extends Component<CategoryItemProps, State> {
    static getDerivedStateFromError(): {
        hasError: boolean;
    };
    state: {
        hasError: boolean;
    };
    componentDidCatch(error: any): void;
    render(): JSX.Element | null;
}
export interface CategoryItemProps extends CategoryItemSchema, StyledLinkProps {
}
export interface CategoryItemSchema {
    categoryId: number;
    text?: string;
}
interface State {
    hasError: boolean;
}
export default CategoryItem;
