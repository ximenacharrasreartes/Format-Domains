import { CssHandlesTypes } from 'vtex.css-handles';
declare const CSS_HANDLES: readonly ["submenu", "submenuWrapper", "submenuContainer"];
export interface SubmenuProps extends SubmenuSchema {
    isOpen: boolean;
    orientation: Orientation;
    paddingTop: number | string;
    paddingBottom: number | string;
    blockClass?: string;
    classes?: CssHandlesTypes.CustomClasses<typeof CSS_HANDLES>;
}
export declare type TachyonsScaleInput = string | number | undefined;
declare const Submenu: StorefrontFunctionComponent<SubmenuProps>;
declare enum Orientation {
    horizontal = "horizontal",
    vertical = "vertical"
}
interface SubmenuSchema {
    width: '100%' | 'auto';
}
export default Submenu;
